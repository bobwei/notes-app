import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  bar: {
    height: 1,
    backgroundColor: '#eee',
  },
  value: {
    height: 1,
    backgroundColor: 'red',
  },
});

export default styles;
