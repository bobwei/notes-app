import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  message: {
    padding: 12,
  },
  text: {
    fontSize: 16,
    marginTop: 5,
    marginBottom: 5,
  },
});

export default styles;
